# Cloudflare Pages SPA routing for pow3r.cashout
# All routes should serve index.html for client-side routing

# API routes go to Cloudflare Pages Functions
/api/* 200

# All other routes serve the SPA
/* /index.html 200

