import React from 'react'
import ReactDOM from 'react-dom/client'
import ComponentLibrary from '../ComponentLibrary.v3.minimal'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ComponentLibrary />
  </React.StrictMode>,
)
